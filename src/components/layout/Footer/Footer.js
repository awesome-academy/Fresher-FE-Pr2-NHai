import { Box, Typography } from '@mui/material';

function Footer() {
  return (
    <Box sx={{ width: '100%' }}>
      <Typography>Footer</Typography>
    </Box>
  );
}

export default Footer;
